# 🐍 Python Input Function Demo

## 📘 Overview
This project demonstrates how to use the **`input()`** function in Python to receive user input during program execution.  
The `input()` function allows interaction between the user and the program by taking data from the user as a string.

---

## 🧠 What is `input()`?
The `input()` function is a **built-in Python function** used to read a line of text entered by the user.

### 🔹 Syntax:
```python
name = input("Enter your name: ")

